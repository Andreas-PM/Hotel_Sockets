package client;

// Sourced from Practical 4 - Clients chat

// Class which starts the client
public class startClient {
    public static void main(String[] args) {
        // Declare new object of a chat client
        ChatClient cc = new ChatClient();
        cc.startClient();
    }
}
